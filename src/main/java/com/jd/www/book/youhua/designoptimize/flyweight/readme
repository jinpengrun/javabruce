享元模式
对性能提升的主要帮助有两点
1 节省创建对象的开销，因为享元维护的相同对象只会被创建一次，当创建对象耗时时，可以节省大量时间
2 创建对象数量减少，系统内存需求也减小，gc压力也相对减小，使系统有一个更健康的内存结构

角色组成：享元工厂，抽象享元，具体享元类和主函数

享元模式是为数不多的，只为提升系统性能而生的设计模式，主要作用就是复用大对象，节省内存空间和对象创建时间
典型应用saas系统中
例如甲乙丙为saas系统的用户，则定义每个公司为这套系统中的一个租户

使用享元模式为每个租户分别提供工资查询的接口，而一个公司（租户）下所有员工可以共享一个查询（每个租户下的
员工数据都存放在一个数据库中，他们共享数据库连接），这样系统只需要3个享元实例，就足以应付300员工的查询
请求了


享元模式和对象池的最大区别是
享元对象是不可替换的 各自有各自的含义和用途，而对象池中的 对象都是等价。


